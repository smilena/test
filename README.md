## Back

In `/server/api` folder , run:

### `npm install`
### `node index.js`

## Front

In `/client` folder, run:

### `yarn install`
### `yarn start`

Run to build the app for production to the `build` folder.

### `yarn build`

